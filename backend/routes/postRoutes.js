const express = require('express');
const router = express.Router();
// const postController = require('../controllers/postController');
// const authMiddleware = require('../middleware/authMiddleware');

// TODO: Implement post routes
// router.get('/', postController.getAllPosts);
// router.get('/:id', postController.getPostById);
// router.post('/', authMiddleware.protect, authMiddleware.authorOnly, postController.createPost);
// router.put('/:id', authMiddleware.protect, authMiddleware.authorOnly, postController.updatePost);
// router.delete('/:id', authMiddleware.protect, authMiddleware.authorOnly, postController.deletePost);

module.exports = router; 