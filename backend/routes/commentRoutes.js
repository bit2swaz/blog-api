const express = require('express');
const router = express.Router();
// const commentController = require('../controllers/commentController');
// const authMiddleware = require('../middleware/authMiddleware');

// TODO: Implement comment routes
// router.get('/post/:postId', commentController.getCommentsByPostId);
// router.post('/', authMiddleware.protect, commentController.createComment);
// router.put('/:id', authMiddleware.protect, commentController.updateComment);
// router.delete('/:id', authMiddleware.protect, commentController.deleteComment);

module.exports = router; 